package ${package};

import ${apiPackage}.impl.Default${apiName};

/**
 * <p>
 * Implementation of your new Plugin API. This class extends the default implementation, that is often provided by APIs.
 *
 * @author <a href="mailto:${yourMail}">${yourName}</a>
 *
 */
public class ${name}UsingDefaultImpl extends Default${apiName} {
    
	//TODO implement
	
}