package ${package};

import org.apromore.plugin.PropertyAwarePlugin;

/**
 * A Plugin API template extending {@see PropertyAwarePlugin}. 
 * If you don't need this extended functionality you may consider to just extend from {@see Plugin}.
 *
 * @author <a href="mailto:${yourMail}">${yourName}</a>
 *
 */
public interface ${name} extends PropertyAwarePlugin {
    
	void doSomething();
	
}