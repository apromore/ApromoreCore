<?xml version="1.0" encoding="UTF-8"?>


<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">

    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>org.apromore</groupId>
        <artifactId>apromore</artifactId>
        <version>1.1</version>
        <relativePath>../../</relativePath>
    </parent>

    <properties>
        <checkstyle.skip>false</checkstyle.skip>
    </properties>

    <artifactId>rest-endpoint-jax-rs</artifactId>
    <version>1.0</version>
    <packaging>war</packaging>
    <name>REST endpoint (JAX-RS)</name>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.felix</groupId>
                <artifactId>maven-bundle-plugin</artifactId>
                <extensions>true</extensions>
                <configuration>
                    <instructions>
                        <Import-Package>
                            <!-- Spring security
                            org.aopalliance.aop,
                            org.apromore.portal.servlet,
                            org.apromore.security,
                            org.apromore.security.filter,
                            org.apromore.security.provider,
                            org.springframework.aop,
                            org.springframework.aop.framework,
                            org.springframework.remoting.httpinvoker,
                            org.springframework.security.core.userdetails,
                            org.springframework.security.remoting.httpinvoker,
                            org.springframework.security.web.authentication,
                            org.springframework.security.web.session,
                            org.springframework.web.filter,
                            -->

                            <!-- Spring DM classes dynamically referenced in web.xml -->
                            org.eclipse.virgo.web.dm;resolution:=optional,  <!-- Virgo -->
                            org.springframework.osgi.web.context.support;resolution:=optional,  <!-- Karaf -->
                            org.springframework.web.context,
                            org.springframework.web.context.request,

                            *
                        </Import-Package>
                        <DynamicImport-Package>
                            com.sun.jersey.*  <!-- Jersey implementation of JAX-RS dynamically loaded -->
                        </DynamicImport-Package>
                        <Web-ContextPath>/rest</Web-ContextPath>
                    </instructions>
                </configuration>
            </plugin>

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-war-plugin</artifactId>
                <configuration>
                    <packagingExcludes> <!-- Regex to only include the same embedded dependencies as specified in maven-bundle-plugin -->
                        %regex[WEB-INF/lib/.*jar]
                    </packagingExcludes>
                    <archive>
                        <manifestFile>${project.build.outputDirectory}/META-INF/MANIFEST.MF</manifestFile>
                    </archive>
                </configuration>
            </plugin>
        </plugins>
    </build>

    <dependencies>
        <dependency>
            <groupId>javax.annotation</groupId>
            <artifactId>javax.annotation-api</artifactId>
            <version>1.3.2</version>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>javax.servlet</groupId>
            <artifactId>javax.servlet-api</artifactId>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>javax.ws.rs</groupId>
            <artifactId>jsr311-api</artifactId>
            <version>1.1.1</version>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>org.apromore</groupId>
            <artifactId>apromore-manager</artifactId>
            <version>1.1</version>
        </dependency>
        <dependency>
            <groupId>org.apromore</groupId>
            <artifactId>portal-model</artifactId>
            <version>1.1</version>
        </dependency>
        <dependency>
            <groupId>org.osgi</groupId>
            <artifactId>org.osgi.compendium</artifactId>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>org.slf4j</groupId>
            <artifactId>slf4j-api</artifactId>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.security</groupId>
            <artifactId>org.springframework.security.core</artifactId>
        </dependency>

        <dependency>
            <groupId>junit</groupId>
            <artifactId>junit</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>
</project>
